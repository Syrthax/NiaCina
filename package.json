{
  "name": "hiresume",
  "version": "1.0.0",
  "description": "Resume builder that pulls in your GitHub projects",
  "scripts": {
    "start": "python3 serve.py 4173",
    "build": "rm -rf dist && mkdir dist && cp index.html script.js styles.css logo.svg dist/",
    "deploy": "npm run build && wrangler pages deploy dist --project-name hiresume --branch main",
    "test": "echo \"No automated tests configured\""
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/Syrthax/NiaCina.git"
  },
  "keywords": [],
  "author": "",
  "license": "ISC"
}
